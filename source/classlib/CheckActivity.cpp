#include "win4me.h"
#include "CheckActivity.h"

bool CheckActivity::Count(uint Days,uint CountableAmount,
                          uint CountableAccum,
                          uint& NumTrans,uint& Amount,
                          char* Account,char* Station,char* Sic)

{

}